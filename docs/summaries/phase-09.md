### ✅ Phase 9 Summary: التكامل بين الوحدات (القلب)

**- What was done:**
- **خدمة موحّدة للربط** `src/lib/integration.ts`:
  - `أنشئ_عملية_مرتبطة(tx, …)`: معاملة ذرّية واحدة — **تحصيل** (إيراد خزنة + قيد دائن على العميل، رصيده −X) أو **صرف** (مصروف خزنة + قيد مدين على المورد، المستحق −Y)، مع ربط `LedgerEntry.treasuryTxnId` (1‑1) وإعادة حساب الجانبين.
  - `اعكس_عملية_مرتبطة(tx, …)`: حذف القيد + حركة الخزنة معاً وإعادة حساب الطرف والحساب (يُرجِع البيانات لإعادة التطبيق).
- **إجراءات التكامل** `_integration/actions.ts`: `سجّل_عملية_مرتبطة` (إنشاء)، `عدّل_عملية_مرتبطة` و`احذف_عملية_مرتبطة` بسياسة **عكس ثم إعادة تطبيق** + سجل عمليات.
- **إعادة هيكلة بلا تكرار:** `سجل_دفعة` (المرحلة 4) صار يفوّض للخدمة الموحّدة؛ تعديل/حذف حركة الخزنة المرتبطة (المرحلة 5) صار يمرّ عبر العكس/إعادة التطبيق فيعالج الجانبين معاً.
- **الفاتورة → العميل** (المرحلة 6): قيد مدين مرتبط بالرقم، مع عكس/إعادة عند التعديل/الحذف.
- واجهة الخزنة الآن تعدّل/تحذف العمليات المرتبطة (مع شارة "مرتبطة").

**- Files added/changed:** `src/lib/integration.ts, src/app/(app)/_integration/actions.ts, src/app/(app)/_parties/actions.ts (تفويض سجل_دفعة), src/app/(app)/treasury/{actions.ts,client.tsx}, scripts/selftest-phase9.ts`.

**- How to run & test:**
```bash
npx tsx scripts/selftest-phase9.ts   # ✅ السيناريو المرجعي الكامل
```
- **اختبار القبول (تم آلياً — السيناريو الحرفي):** أحمد 0 → فاتورة 185,000 → 185,000 → تحصيل 50,000 عبر البنك → البنك +50,000، الإجمالي +50,000، أحمد 135,000 → تعديل التحصيل إلى 60,000 → البنك +60,000، أحمد 125,000 → حذف التحصيل → عكس كامل (البنك يعود، أحمد 185,000). **لا صفوف يتيمة**، وسجل العمليات يوثّق كل خطوة.

**- Business rules / decisions / assumptions (قرارات اتخذتها):**
1. **[قرار معتمد] سياسة عكس-ثم-إعادة-تطبيق** لكل تعديل/حذف لعملية مرتبطة → اتساق تام للجانبين.
2. **[قرار] خدمة واحدة موحّدة** تستدعيها كل من شاشة الطرف وشاشة الخزنة (لا منطق مكرّر).
3. **[قرار] الرابط 1‑1** بين `LedgerEntry` و`TreasuryTxn` عبر `treasuryTxnId` الفريد؛ الحذف يحذف الطرفين (نحذف القيد أولاً ثم الحركة).
4. **[افتراض] الاتجاه يُشتق من نوع الطرف** (عميل→تحصيل/إيراد، مورد→صرف/مصروف).

**- Missing / deferred:** لا شيء جوهري؛ تشديد إضافي اختياري (أقفال متزامنة) غير مطلوب على هذا الحجم.

**- Questions I need answered before continuing:** لا شيء.
