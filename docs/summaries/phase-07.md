### ✅ Phase 7 Summary: الشيكات (CRUD + تجميع شهري + تنبيهات)

**- What was done:**
- **`/cheques`**: جدول الشيكات (اسم المدين، المبلغ، المستفيد، محوّل من، البنك، الاستحقاق، رقم الشيك، الحالة) — بحث/فرز/ترقيم.
- **إضافة/تعديل/حذف** (Server Actions + Zod + `can()` + سجل عمليات + طوابع). الحالة عبر قائمة اختيار (منتظر/محصّل/مرتجع) + **تغيير الحالة المباشر** من الصف.
- **التجميع الشهري**: تبويبات لكل شهر استحقاق، كل تبويب يعرض **عدد الشيكات + إجمالي القيمة**، بالإضافة لتبويب "الكل".
- **المتأخر**: (الاستحقاق < اليوم والحالة منتظر) يظهر بالأحمر مع شارة "متأخر"؛ تحويل الحالة لمحصّل يزيله من المتأخر.
- **التنبيهات** (`src/lib/cheques.ts`، مهيّأة للوحة المرحلة 10): تستحق خلال 7 أيام، خلال الشهر، المتأخرة، إجمالي المستحق + قائمة المتأخرة — معروضة كبطاقات أعلى الصفحة.
- **تخزين صورة الشيك** في Postgres (Bytes) — مكوّن الرفع `حقول_OCR_للشيك` (رفع + معاينة + التقاط بالكاميرا على الموبايل) وزر "استخراج تلقائي" (يستدعي `/api/cheques/ocr` — يُنفَّذ في المرحلة 8). مسار `/api/cheques/[id]/image` لعرض الصورة المخزّنة.

**- Files added/changed:** `src/lib/cheques.ts, src/lib/schemas/cheque.ts, src/app/(app)/cheques/{page,client,ocr-upload}.tsx, src/app/(app)/cheques/actions.ts, src/app/api/cheques/[id]/image/route.ts, scripts/selftest-phase7.ts`.

**- How to run & test:**
```bash
npx tsx scripts/selftest-phase7.ts   # ✅ المتأخر + التجميع الشهري + التنبيهات
npm run dev  # /cheques → إضافة شيكات عبر أشهر → تبويبات + متأخر أحمر
```
- **اختبار القبول (تم آلياً):** شيكات عبر 3 أشهر → مجموعات شهرية صحيحة وإجماليات لكل شهر؛ شيك منتظر فات تاريخه يظهر متأخراً؛ المحصّل ليس متأخراً؛ البحث/الفرز يعملان.

**- Business rules / decisions / assumptions (قرارات اتخذتها):**
1. **[قرار معتمد] صورة الشيك في Postgres** (Bytes + mime) لتبقى على Railway.
2. **[قرار] التجميع عبر تبويبات شهرية** (بدل أكورديون) لوضوح أكبر مع تبويب "الكل".
3. **[قرار] تغيير الحالة مباشرة** من الصف بقائمة اختيار لسرعة العمل.
4. **[افتراض] حقول المستفيد/محوّل من/البنك/رقم الشيك اختيارية** (قد لا تتوفر دائماً).

**- Missing / deferred:** تنفيذ محرّك OCR الفعلي `/api/cheques/ocr` (المرحلة 8) — واجهة الرفع جاهزة.

**- Questions I need answered before continuing:** لا شيء.
