### ✅ Phase 4 Summary: العملاء والموردون + دفتر الأستاذ والمدفوعات

**- What was done:**
- **منطق دفتر الأستاذ** `src/lib/ledger.ts`:
  - اتفاقيات الرصيد كما في CLAUDE.md: عميل = Σمدين−Σدائن (موجب=مديونية)؛ مورد = Σدائن−Σمدين (موجب=مستحق للمورد).
  - `أعد_حساب_سلسلة_الطرف(tx, id)`: تعيد حساب "الرصيد بعد الحركة" ترتيباً زمنياً وتحدّث `Party.balance` (تُستدعى بعد كل إضافة/تعديل/حذف).
  - `أضف_قيد(tx, …)`: إنشاء قيد + إعادة حساب داخل نفس المعاملة.
  - `وصف_الرصيد`: تسمية/لون (مديونية/مستحق/دفعة مقدمة/مسدّد).
- **قوائم `/customers` و`/suppliers`** (DataTable: الاسم، الهاتف، الرصيد مع تسمية ولون، إجراءات) — بحث/فرز/ترقيم.
- **إضافة/تعديل/حذف طرف** (Server Actions + Zod مشترك + `can()` + سجل عمليات + طوابع). منع حذف طرف له حركات/فواتير برسالة عربية.
- **صفحة الطرف `/customers/[id]` و`/suppliers/[id]`** (قالب موحّد): رأس بالمعلومات + سطر المساءلة + سجل التغييرات + 3 بطاقات مؤشر (إجمالي الفواتير/المشتريات، إجمالي المدفوعات، الرصيد الحالي) + جدول الحركات (التاريخ، المستند، البيان، الكمية، مدين، دائن، الرصيد بعد الحركة — ترتيب زمني تصاعدي).
- **تسجيل دفعة**: التاريخ (افتراضي اليوم) + المبلغ + طريقة الدفع + حساب الخزنة، ينشئ قيد الطرف فقط (عميل: دائن؛ مورد: مدين) داخل `$transaction`، مع **نقطة تكامل واضحة (TODO المرحلة 9)** لجانب الخزنة دون تكرار المنطق.
- **حركة يدوية** (رصيد افتتاحي/تسوية) + **حذف حركة** مع إعادة حساب السلسلة. تحذير الدفع الزائد (رصيد عميل سالب) مسموح مع تنبيه.

**- Files added/changed:** `src/lib/ledger.ts, src/lib/schemas/party.ts, src/app/(app)/_parties/{actions.ts,list-client.tsx,detail.tsx,detail-client.tsx}, src/app/(app)/{customers,suppliers}/page.tsx, src/app/(app)/{customers,suppliers}/[id]/page.tsx, scripts/selftest-phase4.ts`.

**- How to run & test:**
```bash
npx tsx scripts/selftest-phase4.ts   # ✅ السيناريو الرقمي
npm run dev  # /customers → إضافة عميل → فتحه → حركة يدوية مدين 185000 → تحصيل 50000
```
- **اختبار القبول (تم آلياً):** رصيد 0 → مدين 185,000 → 185,000 → دفعة 50,000 → 135,000، والرصيد الجاري صحيح في الحركات. حذف الدفعة يعيد الرصيد إلى 185,000 (إعادة حساب). مرآة المورد: 100,000 ثم 70,000.

**- Business rules / decisions / assumptions (قرارات اتخذتها):**
1. **[قرار] إعادة حساب كاملة للسلسلة** بعد كل تغيير (O(n)) — أبسط وأضمن للصحة على هذا الحجم؛ مع الرصيد المخزّن في `Party.balance`.
2. **[قرار] ترتيب الحركات تصاعدي زمنياً** (الأقدم أولاً) ليُقرأ الرصيد الجاري بشكل طبيعي.
3. **[قرار] "حركة يدوية"** لإدخال الأرصدة الافتتاحية/التسويات (مدين/دائن مباشرة).
4. **[قرار] الدفع الزائد مسموح** مع تنبيه (يصبح الرصيد "دفعة مقدمة").
5. **[افتراض] الحركات المرتبطة بفاتورة/خزنة لا تُحذف يدوياً** من هنا؛ تُدار عبر مصدرها (الفاتورة/التكامل) — المراحل 6 و9.

**- Missing / deferred:**
- ربط الدفعة بالخزنة فعلياً (المرحلة 9) — النقطة جاهزة.
- تعديل قيد فردي عبر الواجهة (متاح حذف + إعادة إنشاء؛ التعديل الكامل للقيود المرتبطة في المرحلة 9).

**- Questions I need answered before continuing:** لا شيء.
