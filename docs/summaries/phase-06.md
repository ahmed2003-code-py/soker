### ✅ Phase 6 Summary: الفواتير (تجميع تلقائي + طباعة/PDF + ربط دفتر الأستاذ)

**- What was done:**
- **منطق الفواتير** `src/lib/invoice.ts`:
  - `احصل_رقم_فاتورة_جديد(tx)`: ترقيم تسلسلي فريد آمن للتزامن عبر تحديث ذرّي SQL لعدّاد الإعدادات (`RETURNING`).
  - `احسب_إجماليات`: **التسعير بالوزن** — مجموع البند = السعر × الوزن؛ والإجمالي = Σ البنود.
  - `جمّع_حسب_التصنيف`: تجميع العدد/الوزن لكل تصنيف.
  - `رحّل_فاتورة_للعميل` / `اعكس_قيود_الفاتورة`: ترحيل قيمة الفاتورة قيداً مديناً على العميل مرتبطاً برقمها + إعادة حساب الرصيد، وعكسه عند التعديل/الحذف.
- **`/invoices`** قائمة (الرقم مصفّر 0000000، العميل، التاريخ، الوزن، الإجمالي) بحث/فرز/ترقيم.
- **`/invoices/new`** نموذج كامل الصفحة: عميل (قائمة اختيار + **إضافة عميل جديد inline**)، الهاتف، التاريخ، بنود ديناميكية (إضافة/حذف، **Enter يضيف بنداً**، تحديد عند التركيز للأرقام)، التصنيف (قائمة + **تصنيف جديد inline**)، **إجماليات وملخص تجميع حيّ**.
- **`/invoices/[id]`** عرض مطابق لشكل الفاتورة الورقية: شعار/اسم الشركة (من الإعدادات)، رقم/تاريخ، العميل/الهاتف، جدول البنود، ملخص التجميع، الإجمالي + **"فقط وقدره" بالعربية (تفقيط)** — مع **طباعة عبر @media print نظيفة (A4)** و"حفظ PDF".
- **`/invoices/[id]/edit`** نفس النموذج معبّأً. **حفظ الفاتورة (في `$transaction`)** ينشئ قيد العميل؛ التعديل يعكس ثم يعيد الترحيل (يُحدّث رصيد العميل القديم والجديد)؛ الحذف يعكس القيد. كله مع سجل عمليات.
- `src/lib/tafqit.ts`: تحويل المبلغ إلى كلمات عربية (جنيه/قروش).
- إخفاء هيكل التطبيق عند الطباعة (`no-print`).

**- Files added/changed:** `src/lib/{invoice,tafqit}.ts, src/lib/schemas/invoice.ts, src/app/(app)/invoices/{page,list-client,form}.tsx, src/app/(app)/invoices/actions.ts, src/app/(app)/invoices/new/page.tsx, src/app/(app)/invoices/[id]/{page,actions-bar}.tsx, src/app/(app)/invoices/[id]/edit/page.tsx, src/components/shell/app-shell.tsx (no-print), scripts/selftest-phase6.ts`.

**- How to run & test:**
```bash
npx tsx scripts/selftest-phase6.ts   # ✅ التجميع + الترقيم + التسعير بالوزن + الربط + تعديل/حذف
npm run dev  # /invoices/new → بنود في 14×1، 28×2، برج → حفظ → عرض/طباعة
```
- **اختبار القبول (تم آلياً):** فاتورة ببنود 14×1/28×2/برج → التجميع صحيح، الإجمالي 185,000 (وزن×سعر)، رقم تسلسلي 5651→5652، العميل يحصل على قيد مدين=قيمة الفاتورة، التعديل/الحذف يعكسان القيد ويحدّثان الرصيد.

**- Business rules / decisions / assumptions (قرارات اتخذتها):**
1. **[قرار معتمد] التسعير بالوزن** (مجموع البند = السعر × الوزن).
2. **[قرار] العميل من القائمة فقط** مع إتاحة "إضافة عميل جديد" inline داخل قائمة الاختيار (بدل النص الحر) — يضمن ربط دفتر الأستاذ.
3. **[قرار معتمد] التعديل/الحذف مسموح** بسياسة **عكس ثم إعادة ترحيل** لإبقاء الأرصدة متسقة + تسجيل.
4. **[قرار] الترقيم عبر عدّاد ذرّي** (فريد، آمن للتزامن؛ قد تحدث فجوات عند التراجع وهذا مقبول).
5. **[ملاحظة] عدّاد الفواتير** تقدّم أثناء الاختبار؛ سيُعاد ضبطه ضمن بيانات العرض في المرحلة 13.

**- Missing / deferred:** التشديد الكامل للتكامل (المرحلة 9). التصنيف يُخزَّن كنص على البند (لا جدول مستقل) — كافٍ للمطلوب.

**- Questions I need answered before continuing:** تأكيد قاعدة التسعير (بالوزن) عند المراجعة — أتابع بالوزن.
